apiVersion: 1

providers:
  - name: 'SentinelPHP'
    orgId: 1
    folder: 'SentinelPHP'
    folderUid: 'sentinel'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
